---
summary: "Task5513 prepared an actual-native integration harness; artifact absence stopped execution, with no completion claim."
read_when:
  - "Resuming Decision151 integration after the producer exports new native binaries."
---

# Decision151 / task5513 — native integration preparation

Built root-only callbacks composing the actual AK supervisor/compiled-worker seam
and Pi sealed startup/SDK host, with independent wire/state/OFD oracles. Fifteen
real-process case definitions remain unexecuted. Eight static/oracle-unit checks
pass; the actual artifact gate exits 78 because both historical binaries were
runner-cleaned. No build fallback, scripted native ready, AK call or live effect.

Current producer source differs from its historical receipt and Pi I04 is changing
concurrently. Re-freeze owner source/artifacts before execution. The external native
fixture still needs bounded claim-statement/readback fault operations; alias coverage
needs frozen I04. Public-entrypoint/installed/reality/full-effect proof remains separate.
The runner explicitly refuses completion while required gaps remain, even if its
implemented native cases later pass.

[Execution boundary, files, evidence and resume commands](../docs/project/2026-09-07-visible-task-session-native-integration.md).
Parent retains task5513/final5482 authority. Original checkout and other workers' source
were not modified; no canonical repo/worktree identity was rebound.
