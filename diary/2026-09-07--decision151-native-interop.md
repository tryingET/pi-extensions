---
summary: "Task5513 executed 16 actual native AK/Pi interop cases; frozen schema43 proof passes while current/deployed gates remain pending."
read_when:
  - "Resuming the native schema40/fault-control integration continuation."
---

# Decision151 / task5513 — actual interop

The available exported native worker was exercised, not replaced by a scripted ready
fixture. Sixteen cases passed against exact d719 source/export and the I04 reviewed
Pi tarball, including actual native claim → T1 → T2/CLOSED → serialized SDK send/tool,
nonbuiltin model identity, retained failures and explicit native recovery.

The moving working trees were not an excuse to wait or a reason to ignore currentness:
we copied a hash-verified historical verification packet. 184 AK files came from Git;
one explicitly labelled ignored build-settings appendix matched the export manifest.
No build, canonical DB, live provider/config/activation or other-worker action occurred.

An import-only ESM resolution error and fake-viewer timer leak were harness defects,
not production findings. Both failed experiments were retained and corrected before
final native runs. Unavailable schema40/statement/readback controls and the later I04
reasoning-map repair remain pending. The wrapper still returns 78 rather than minting
completion from passing subset evidence.

[Exact artifacts, case outcomes, evidence and resume commands](../docs/project/2026-09-07-visible-task-session-native-interop.md).
