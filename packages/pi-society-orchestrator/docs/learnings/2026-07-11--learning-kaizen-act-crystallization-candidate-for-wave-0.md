---
summary: "KES learning candidate for kaizen act crystallization candidate for Wave 0 safety foundations in /home/tryinget/ai-society/core/rocs-cli, prerequisite to Waves 1-4. Implement and dogfood only the deterministic trust foundation needed before any superintelligence integration: (1) remediation apply must reject stale/fabricated scorecard semantics by strict versioned schema/digests and fresh deterministic re-observation; (2) fleet audit must cryptographically verify vendored file manifests, reject empty/incomplete/traversal/symlink/malformed/extra-file cases as appropriate; (3) CI cleanup must resolve and contain every deletion beneath a verified repo root; (4) mutation paths for vendoring/bootstrap/nightly artifacts must use staged atomic publication, locking/preimages/rollback where practical; (5) policy schemas require exact booleans, known capabilities, unique normalized repos; (6) tests must cover forged inputs, stale digests, path escape, failures, concurrency/collision where practical. Keep current script interfaces during Wave 0 only because Wave 1 will delete/dissolve them; do not create long-lived compatibility shims. Preserve unrelated pre-existing AGENTS.md and uv.lock changes. Update CHANGELOG for any breaking behavior. Run the full unit suite and direct disposable-fixture dogfooding demonstrating both rejection and success paths. Do not commit; later waves follow."
read_when:
  - "Reviewing a package-owned learning candidate before promotion."
kes_contract_version: 1
kes_kind: "learning_candidate"
kes_package: "pi-society-orchestrator"
system4d:
  container: "Package-local KES learning candidate."
  compass: "Bound promotion from raw capture into a durable candidate without inventing a second authority surface."
  engine: "Tie the claim to raw evidence -> state reusable heuristics -> capture follow-up and anti-patterns."
  fog: "The main risk is promoting pattern language without attributable package-local evidence."
---

# 2026-07-11 — KES Learning Candidate: kaizen act crystallization candidate for Wave 0 safety foundations in /home/tryinget/ai-society/core/rocs-cli, prerequisite to Waves 1-4. Implement and dogfood only the deterministic trust foundation needed before any superintelligence integration: (1) remediation apply must reject stale/fabricated scorecard semantics by strict versioned schema/digests and fresh deterministic re-observation; (2) fleet audit must cryptographically verify vendored file manifests, reject empty/incomplete/traversal/symlink/malformed/extra-file cases as appropriate; (3) CI cleanup must resolve and contain every deletion beneath a verified repo root; (4) mutation paths for vendoring/bootstrap/nightly artifacts must use staged atomic publication, locking/preimages/rollback where practical; (5) policy schemas require exact booleans, known capabilities, unique normalized repos; (6) tests must cover forged inputs, stale digests, path escape, failures, concurrency/collision where practical. Keep current script interfaces during Wave 0 only because Wave 1 will delete/dissolve them; do not create long-lived compatibility shims. Preserve unrelated pre-existing AGENTS.md and uv.lock changes. Update CHANGELOG for any breaking behavior. Run the full unit suite and direct disposable-fixture dogfooding demonstrating both rejection and success paths. Do not commit; later waves follow.

## Status
- State: candidate-only
- Candidate kind: learning

## Source
- Package: pi-society-orchestrator
- Source diary: `diary/2026-07-11--phase-kaizen-act-kaizen-act-phase-for-wave-0-safety-found.md`
- Source kind: loop_phase
- Loop: kaizen
- Phase: act
- Session: kaizen-1783741676783
- Objective: Wave 0 safety foundations in /home/tryinget/ai-society/core/rocs-cli, prerequisite to Waves 1-4. Implement and dogfood only the deterministic trust foundation needed before any superintelligence integration: (1) remediation apply must reject stale/fabricated scorecard semantics by strict versioned schema/digests and fresh deterministic re-observation; (2) fleet audit must cryptographically verify vendored file manifests, reject empty/incomplete/traversal/symlink/malformed/extra-file cases as appropriate; (3) CI cleanup must resolve and contain every deletion beneath a verified repo root; (4) mutation paths for vendoring/bootstrap/nightly artifacts must use staged atomic publication, locking/preimages/rollback where practical; (5) policy schemas require exact booleans, known capabilities, unique normalized repos; (6) tests must cover forged inputs, stale digests, path escape, failures, concurrency/collision where practical. Keep current script interfaces during Wave 0 only because Wave 1 will delete/dissolve them; do not create long-lived compatibility shims. Preserve unrelated pre-existing AGENTS.md and uv.lock changes. Update CHANGELOG for any breaking behavior. Run the full unit suite and direct disposable-fixture dogfooding demonstrating both rejection and success paths. Do not commit; later waves follow.

## Claim
The kaizen act phase surfaced reusable material for "Wave 0 safety foundations in /home/tryinget/ai-society/core/rocs-cli, prerequisite to Waves 1-4. Implement and dogfood only the deterministic trust foundation needed before any superintelligence integration: (1) remediation apply must reject stale/fabricated scorecard semantics by strict versioned schema/digests and fresh deterministic re-observation; (2) fleet audit must cryptographically verify vendored file manifests, reject empty/incomplete/traversal/symlink/malformed/extra-file cases as appropriate; (3) CI cleanup must resolve and contain every deletion beneath a verified repo root; (4) mutation paths for vendoring/bootstrap/nightly artifacts must use staged atomic publication, locking/preimages/rollback where practical; (5) policy schemas require exact booleans, known capabilities, unique normalized repos; (6) tests must cover forged inputs, stale digests, path escape, failures, concurrency/collision where practical. Keep current script interfaces during Wave 0 only because Wave 1 will delete/dissolve them; do not create long-lived compatibility shims. Preserve unrelated pre-existing AGENTS.md and uv.lock changes. Update CHANGELOG for any breaking behavior. Run the full unit suite and direct disposable-fixture dogfooding demonstrating both rejection and success paths. Do not commit; later waves follow."; review the linked diary entry before promoting it beyond this package.

## Evidence
- Phase status: done.
- Primary cognitive tool: knowledge-crystallization.
- Captured output excerpt: ## Patterns Discovered - Untrusted remediation inputs require both cryptographic integrity and fresh deterministic re-observation before mutation. - Vendored integrity requires exact manifest-to-filesystem coverage, not…

## Reusable Heuristics
- Promote only after confirming the candidate still matches the full raw diary capture.

## Anti-patterns to Avoid
- Do not treat candidate-only KES output as a canonical learning without review.
- Do not promote failed or partial loop output beyond the linked diary evidence.

## Follow-up
- Review the linked diary entry and explicitly decide whether to elevate this candidate.

## Metadata
```json
{
  "kes_contract_version": 1,
  "package": "pi-society-orchestrator",
  "source": {
    "kind": "loop_phase",
    "loop": "kaizen",
    "phase": "act",
    "sessionId": "kaizen-1783741676783",
    "objective": "Wave 0 safety foundations in /home/tryinget/ai-society/core/rocs-cli, prerequisite to Waves 1-4. Implement and dogfood only the deterministic trust foundation needed before any superintelligence integration: (1) remediation apply must reject stale/fabricated scorecard semantics by strict versioned schema/digests and fresh deterministic re-observation; (2) fleet audit must cryptographically verify vendored file manifests, reject empty/incomplete/traversal/symlink/malformed/extra-file cases as appropriate; (3) CI cleanup must resolve and contain every deletion beneath a verified repo root; (4) mutation paths for vendoring/bootstrap/nightly artifacts must use staged atomic publication, locking/preimages/rollback where practical; (5) policy schemas require exact booleans, known capabilities, unique normalized repos; (6) tests must cover forged inputs, stale digests, path escape, failures, concurrency/collision where practical. Keep current script interfaces during Wave 0 only because Wave 1 will delete/dissolve them; do not create long-lived compatibility shims. Preserve unrelated pre-existing AGENTS.md and uv.lock changes. Update CHANGELOG for any breaking behavior. Run the full unit suite and direct disposable-fixture dogfooding demonstrating both rejection and success paths. Do not commit; later waves follow."
  },
  "sourceDiary": "diary/2026-07-11--phase-kaizen-act-kaizen-act-phase-for-wave-0-safety-found.md",
  "metadata": {
    "event": "phase_candidate",
    "agent": "researcher",
    "primaryTool": "knowledge-crystallization",
    "status": "done"
  }
}
```
