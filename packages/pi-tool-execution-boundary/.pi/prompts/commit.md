---
summary: "Package-local commit workflow."
read_when:
  - "Preparing a commit in this package."
---
# Commit this package
Inspect only package-related paths, run `npm test`, `npm run validate:artifacts`, and the canonical package quality gate. Stage only confirmed package and root release/routing metadata.
