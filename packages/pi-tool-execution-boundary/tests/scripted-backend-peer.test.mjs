import test from"node:test";import assert from"node:assert/strict";import{ScriptedBackendPeer,scriptedEvent}from"./support/scripted-backend-peer.mjs";test("emits deterministic sequenced events",()=>{const p=new ScriptedBackendPeer([scriptedEvent.ready(),scriptedEvent.accepted(),scriptedEvent.started(),scriptedEvent.output("stdout","hi"),scriptedEvent.terminal({processExit:"known"})]);assert.deepEqual(p.drain("c").map(e=>[e.sequence,e.type]),[[1,"ready"],[2,"accepted"],[3,"started"],[4,"output"],[5,"terminal"]]);});test("reproduces disconnect after start",()=>{const p=new ScriptedBackendPeer([scriptedEvent.accepted(),scriptedEvent.started(),scriptedEvent.disconnect("lost")]);p.next("c");p.next("c");assert.throws(()=>p.next("c"),e=>e.code==="SCRIPTED_BACKEND_DISCONNECT");});test("invalid events rejected",()=>assert.throws(()=>new ScriptedBackendPeer([{type:"invented"}]),/invalid scripted event/));
