---
summary: "Product and technical vision for pi-activity-strip."
read_when:
  - "Defining or revisiting project direction."
system4d:
  container: "Project north-star statement."
  compass: "Make terminal-first Pi work legible in one glance across active sessions."
  engine: "Capture live Pi state cheaply, aggregate locally, and render a beautiful always-visible ribbon."
  fog: "Real operator use may demand richer drill-down, multi-monitor behavior, and future pi-server integration."
---

# Vision

Make `pi-activity-strip` the default local visibility layer for active Pi sessions: a small, elegant top-row ribbon that answers **what is every tab doing right now?** without forcing operators out of Ghostty or into a heavier dashboard.
