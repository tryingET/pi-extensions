---
summary: "Package-group scope note for pi-interaction inside the pi-extensions monorepo."
read_when:
  - "You are editing files under packages/pi-interaction/."
system4d:
  container: "Package-group context inside a shared monorepo."
  compass: "Keep changes package-scoped and root-compatible."
  engine: "Read local docs/scripts as needed, then implement and validate."
  fog: "Detailed workflow rules belong in package docs and scripts, not this file."
---

# AGENTS.md

## Scope
`packages/pi-interaction/` is a monorepo package group, not a git root.

## Local rule
Keep this file package-scoped and minimal.
Use local package docs, manifests, and scripts for operational details.
