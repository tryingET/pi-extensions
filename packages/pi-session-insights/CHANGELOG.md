---
summary: "Change history for the private pi-session-insights package."
read_when:
  - "Reviewing package history."
---

# Changelog

## 0.1.0 — unreleased

- Add bounded jq-first extraction for Pi session JSONL.
- Reconstruct active parent chains and handle both `firstKeptEntryId` and `retainedTail` compactions, branches, custom entries, model changes, and thinking-level changes.
- Add first-message-only scout/subagent/fork boot classification without suppressing later operator text.
- Add strict source-qualified authority/runtime/KES/propagation attribution that ignores unsourced values.
- Cap text, metadata strings, chain IDs, task IDs, mutation roots, custom types, and uncertainties with explicit truncation facts.
- Add the package-owned `pi-session-jsonl` skill and fixture suite.
- Keep the first slice private, skill-only, non-live, and `releaseConfigMode=none`.
