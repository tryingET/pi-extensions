---
summary: "Organization authority stays outside this package."
read_when:
  - "Organization authority stays outside this package."
system4d:
  container: "Private pi-typescript-tool package in pi-extensions."
  compass: "Typed trusted-code execution with explicit limits."
  engine: "Review -> implement -> package checks -> owner activation."
  fog: "In-process vm and typechecking are not security boundaries."
---

# Operating model boundary

This package implements a Pi workbench capability. It does not own organization
strategy, task authority, ontology semantics, reusable process authority or
publication policy. Follow the monorepo's root AGENTS and workspace owner
boundaries; use AK in the root repo context for authorized task/direction work.
Package docs explain behavior and evidence, not independent authority state.
