---
summary: "Product and technical vision for the non-live pi-context-corpus package."
read_when:
  - "Defining or revisiting project direction."
system4d:
  container: "Project north-star statement."
  compass: "Files are the store; jq is the DSL; the index is the agent feed."
  engine: "Translate context-core questions into one-line jq answers."
  fog: "Real agent workflows may reshape which projections matter."
---

# Vision

A dependable, non-live corpus package that is easy to understand, validate, and
evolve: deterministic indexing, one pinned jq dispatch convention, and
content-free outputs — so multi-session context questions stay one command away.
