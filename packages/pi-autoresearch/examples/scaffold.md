---
summary: "Small example of the current pi-autoresearch package-shell surface."
read_when:
  - "You want a concrete example of what the shell currently exposes."
system4d:
  container: "Package example artifact."
  compass: "Show the shell surface without pretending the bounded runtime kernel already exists."
  engine: "Inspect command -> inspect tool -> note remaining runtime work."
  fog: "Examples can drift if they silently imply capabilities not yet implemented."
---

# Example: package-shell inspection

Current shell surfaces:

- `/autoresearch`
- `autoresearch_runtime_status`

Example tool result themes:

- phase: `package_shell`
- ready Prompt Vault templates:
  - `pi-autoresearch-setup`
  - `pi-autoresearch-next-hypothesis`
  - `pi-autoresearch-finalize`
- blocked router template:
  - `pi-autoresearch-state-router`
