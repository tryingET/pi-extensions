// summary: re-export the Prompt Vault extension as the package entry point.
// read_when:
//   - tracing how consumers load the pi-vault-client extension.

export { default } from "./extensions/vault.js";
