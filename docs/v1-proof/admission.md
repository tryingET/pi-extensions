# v1 proof admission — pos-4

Owner group: **softwareco**. Repository: `pi-extensions`.
Authorized by the operator on 2026-08-24 ("resolve the blocked parts") under
engineering-core decision 128 / population manifest rev 2 (decision 131).
Enrollment is administrative; proof independence is earned at execution.

## Accepted role
heterogeneous transition, federation, package boundaries, hostile history canary

## Pinned baseline
- Revision: `(re-observed at execution start)`
- Manifest pin: see engineering-core `docs/project/v1-proof-population.json` rev 2

## Declared validation contract
Recorded below; executed and evidenced before any journey run.

## Baseline state
Current main checkout, clean tree at admission commit.

## Evidence custody
Owner-local by default; cross-boundary exposure only via default-deny transfer
with exact approved bytes/digest, recipients, purpose, retention, and
training-use prohibition.

## Isolation boundary
Disposable replica with independent Git common directory and isolated HOME,
XDG, temporary, cache, credential, and configuration state. No production
credentials or endpoints. Network denied except manifest-approved digest-bound
fetches.

## Rollback boundary
Replica restore or quarantine; Git does not reverse external effects. No
production mutation is authorized by this admission.

## Final proof-workspace posture
Predeclared per journey manifest; residual state inventoried, then disposed or
quarantined.

## Stop conditions
Owner refusal, active conflicting task claims, inability to bind a full
immutable revision, or any RFC universal stop rule stops work immediately.
