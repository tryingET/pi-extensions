---
summary: "Tactical goals for the routed cross-package wave at the pi-extensions monorepo root after the seam-first prompt-plane proof, first KES packet, and first TG3 hardening slice landed."
read_when:
  - "You need the medium-sized root waves under the active strategic goal."
  - "You need to know which tactical goal is active now and which cross-package follow-ons are next."
system4d:
  container: "Tactical layer for the active root strategic goal."
  compass: "Decompose only the active strategic goal and keep the wave medium-sized, routed, and owner-truthful."
  engine: "Active strategic goal -> 2-5 tactical goals -> choose one active tactical goal."
  fog: "The main risk is reopening completed lower-plane proof, skipping straight to unrelated backlog, or inventing a new loop-hardening slice before AK says one is ready."
---

# Tactical Goals

Active strategic goal: **SG1 — Bind the next cross-package cognition control-plane wave explicitly and keep package-owner boundaries truthful**

## Tactical goal set

### TG1 — Bind the thin prompt-plane seam between `pi-vault-client` and `pi-society-orchestrator` before broader KES/loop activation
- **State:** done
- **Why this is complete:** the owner-side packet fixed the execution order, and the seam-first wave is landed through repo-local AK tasks `task:1050`, `task:1049`, and `task:1051`.
- **Carry-forward guardrail:** keep raw prompt-plane reads out of orchestrator and treat seam-first work as landed history unless new evidence reopens the boundary.

### TG2 — Activate package-owned KES crystallization outputs in `pi-society-orchestrator` once the prompt-plane seam is bound
- **State:** done
- **Completed by:** repo-local AK tasks `task:1089`, `task:1090`, and `task:1091`
- **Why this is complete:** the bounded KES contract exists, loop execution emits package-owned diary and candidate-only learning artifacts through it, and package checks + installed-package release smoke + root validation prove the path strongly enough to treat it as landed truth.
- **Carry-forward guardrail:** keep KES outputs package-owned in `pi-society-orchestrator`, use bounded `diary/` + candidate-only `docs/learnings/` outputs, and do not reopen this packet as if the proof step were still pending.

### TG3 — Harden loop family/evidence contracts around the KES-first control-plane wave and keep higher-order self explicitly deferred
- **State:** active candidate
- **Latest landed slice:** repo-local AK tasks `task:1107` and `task:1108`
- **Why this remains the next truthful area:** the lower prompt/KES packet is now strong enough to support loop-hardening follow-through, and the first hardening slice already landed by making invalid KES roots fail closed and by strengthening installed-package KES proof under the installed package root.
- **Current execution note:** repo-local AK readiness is currently empty again, so the next move is to reassess readiness rather than synthesize implementation from stale prose.
- **Success signal:** any new slice is bounded in AK before implementation starts, tightens loop-family/evidence semantics without reopening seam or KES proof, and keeps higher-order self explicitly downstream of the lower-plane packet.

## Not the active tactical path

These were truthful earlier or remain explicitly deferred, but they are not the current root tactical path now:
- the seam-first prompt-plane binding wave (`tasks:1050`, `1049`, `1051`), which is completed history
- the first KES packet (`tasks:1089`, `1090`, `1091`), which is also completed history
- the first TG3 hardening slice (`tasks:1107`, `1108`), which is now completed history too
- the initial reduced-form root-policy centralization wave
- the guarded repo-bootstrap concern that was externalized to agent-kernel decision `#8`
- `task:962` PufferLib exploration, which remains explicitly deferred and not the current root execution anchor
- higher-order ASC self follow-on work before TG3 is both justified and task-backed
