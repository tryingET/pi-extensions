---
summary: "Tactical goals for the active cross-package seam/KES/loop strategic wave at the pi-extensions monorepo root."
read_when:
  - "You need the medium-sized root waves under the active strategic goal."
  - "You need to know which tactical goal is active now and which cross-package follow-ons are next."
system4d:
  container: "Tactical layer for the active root strategic goal."
  compass: "Decompose only the active strategic goal and keep the wave medium-sized, routed, and owner-truthful."
  engine: "Active strategic goal -> 2-5 tactical goals -> choose one active tactical goal."
  fog: "The main risk is mixing the seam-first binding, later KES activation, and later loop hardening into one blurry queue or jumping to higher-order self work too early."
---

# Tactical Goals

Active strategic goal: **SG1 — Bind the next cross-package cognition control-plane wave explicitly and keep package-owner boundaries truthful**

## Active tactical goals

### TG1 — Bind the thin prompt-plane seam between `pi-vault-client` and `pi-society-orchestrator` before broader KES/loop activation
- **Why this is active:** the owner-side packet already fixed the execution order: prompt-plane seam first, KES second, loops third, and only then any higher-order self follow-on.
- **Current execution anchor:** repo-local AK tasks `task:1050`, `task:1049`, and `task:1051`.
- **Guardrails:** stop treating raw prompt-plane reads inside orchestrator as an acceptable long-term boundary, and do not pull later KES/loop/self work into this tactical goal.

### TG2 — Activate package-owned KES crystallization outputs in `pi-society-orchestrator` once the prompt-plane seam is bound
- **Why next:** `src/kes/` is still effectively empty, but KES is the next truthful lower-plane move after the prompt-plane boundary is no longer raw/local.
- **Success signal:** loop/runtime work can emit bounded diary -> learning/crystallization outputs without inventing a shadow authority surface.

### TG3 — Harden loop family/evidence contracts around the KES-first control-plane wave and keep higher-order self explicitly deferred
- **Why next:** loop execution exists today, but family/authority/evidence semantics should be tightened only after the prompt/KES base is truthful.
- **Success signal:** exploratory, review, and execution parallelism stop blurring together, and higher-order self remains explicitly downstream of the lower-plane proof.

## Not the active tactical path

These were truthful earlier waves, but they are not the current SG1 tactical path now:
- the initial reduced-form root-policy centralization wave
- the routed package runtime-truth follow-through in `pi-society-orchestrator`
- the guarded repo-bootstrap concern that was externalized to agent-kernel decision `#8`
- `task:962` PufferLib exploration, which remains a real exploratory item but not the current root execution anchor after explicit reprioritization
