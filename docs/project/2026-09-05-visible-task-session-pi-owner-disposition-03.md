---
summary: "Operator-delegated Pi task5463 disposition: sealed local SDK host, same-Codex SSE/no-refresh boundary, actual AK interval custody, canonical operational namespace and additive emitted-ESM ABI."
read_when:
  - "Closing local BOOT, EXCL and PKG contract decisions for Decision151."
type: "owner_disposition"
task_id: 5451
owner_task_id: 5463
---

# Decision151 — local Pi integration and little-helpers disposition

## Provenance and authority

Controller-persisted structured transcription of task5463's operative result and requested source/interval clarifications. Origin: peer `scoutpeer-mtoy26tz-f55e0364`, session `01a073a6-aadd-7219-b7ad-daf061677a4e`, primary communication `3708acfb-1d64-43ea-9639-80ad34026e7c`. Clarifications concern same-provider Codex transport/auth and actual shared-OFD startup custody. Owner verification of this transcription is requested separately.

The interview returned **“Authorize these bounded local owner-disposition tasks”** before timeout; task5463 contract/guardrails and evidence8384 record explicit local proposal-disposition delegation. This is not upstream maintainer approval, ADR acceptance, policy/code change, live activation or an independent RFC review. Parent task/evidence facts were supplied to the investigator; no AK/DB commands, writes, tests, installations, live launches, credential or private runtime/model/auth configuration reads or provider calls were performed.

RFC02 SHA-256 matched before/after: `a97c4acc15dcedeb17596882419f2c8a920918d394428e0b1912f419dee7cb14`. Source HEAD had advanced independently to `0abe8ebe4a9c978af58dedb52470c4f3c163aca7`; unchanged RFC/source hashes, not unrelated HEAD movement, bind the assessment. Little-helpers manifest was dirty but matched the cited hash. No installed feature is inferred.

## Actual local decisions

- **AUTH-01 — ACCEPT local integration fit:** fresh ordinary pending B; no transfer, unclaim-to-launch or identity-assurance upgrade.
- **AUTH-02 — CONDITIONAL concurrence:** accept ordered baseline/claim/readback and host release/closure observations only with the actual AK interval below. This supersedes earlier manual-coordination concurrence. Pi does not supply absence of AK writers or unclaim authority. AK owns its substantive protocol acceptance.
- **BOOT-01 — CONDITIONAL on incorporating the finite allocation below; allocation ACCEPTED locally now:** a sealed **local SDK-hosted AgentSession**, not stock InteractiveMode or an optional extension. No upstream patch/approval is required for this selected bounded composition. Requiring unchanged full stock TUI would be a different unresolved requirement.
- **BOOT-02 — ACCEPT specified profile/effect/send restrictions:** no ambient executable factories, compaction or secondary inputs; actual-send and tool-entry assertions; disclose context, lease and credential-lifetime limits. Exact installed profile compatibility remains affected-use proof.
- **EXCL-01 — ACCEPT local namespace/conflict/short-lock design; CONDITIONAL executable enrollment:** other owners must supply AK/lane/candidate/external-writer custody. No local mutex enrolls unmodified writers.
- **PKG-01 — ACCEPT additive same-source emitted-JS production/test ABI and preserved observer/export policy.** Packing/live proof is later.
- **MIG-01 — ACCEPT package-side non-replacement/no-fallback/inspect-preserving withdrawal; lane participation is separately disposed.**

## Selected owner and module allocation

All paths are prospective, not created or installed:

| Owner | Proposed module responsibility |
|---|---|
| Little-helpers | `src/taskSessionHost.ts`: sealed lifecycle, denial latch and private AgentSession; `taskSessionResources.ts`: literal ordered resource snapshots; `taskSessionDispatch.ts`: provider/tool assertions; `taskSessionUi.ts`: restricted visible transcript/control UI; `taskSessionCodexProvider.ts`: same-Codex guarded provider/auth composition. |
| Little-helpers | `taskSessionLaunchCore.ts`, `taskSessionState.ts`: normalized plan/launch/inspect/watch, durable operational reservations and observations; share the existing transport source, never add a second Ghostty spawner. |
| Pi-society-orchestrator | `src/runtime/task-session-adapter.ts`: versioned AK-specific interpretation/approved owner protocol only. No Agent, provider, execution loop, Ghostty, raw DB or cloned claim algorithm. Exact installed manifest entry/ABI, not arbitrary module path or orchestrator registration barrel. |
| Little-helpers | Emitted public `bin/task-session.js`, additive public core export, internal child/state-operation entries and thin Pi projection. Only internal child startup receives private attempt locator. |
| AK | New native startup/recovery interval supervisor/helper, canonical DB lock and closed no-maintenance operation semantics. |

ASC retains existing subagent/loop execution, cancellation and receipts; this is a narrowly scoped visible task host, not another ASC implementation. UI receives copied status/events and stop/quit, never raw AgentSession/model/runtime/provider/credential objects. The AK adapter receives bounded data/observations, not execution capabilities. Generic little-helpers has no hard dependency on the full extra stack; the installed composition must explicitly resolve the selected adapter's emitted closure.

## Mandatory supported path/refusal map

1. Allocate terminal-default-deny local state before construction. Use a literal loader with **zero executable extension factories**, explicit model/thinking, fresh private SessionManager and frozen in-memory SettingsManager; do not use services' default discovery. Validate diagnostics and install mandatory wrappers before publishing any UI/ingress handle. Empty history/queues and absence of constructor effects are proof obligations, not inferred from flags.
2. Admit one inert literal objective via a private host operation using `prompt(..., {expandPromptTemplates:false})` only after owner interval closure. Expansion=false prevents that submission's command/skill/template path but is not itself a gate; executable input/before-agent-start factories are absent. No initial task argv/stdin/editor body, bootstrap model turn or `preflightResult(true)` proof.
3. Visible UI is an actual SDK AgentSession transcript with model/thinking/cwd/session identity, streamed assistant/tool events, status/inspection and stop/quit. It is not unchanged stock InteractiveMode. Do not bind stock submit, manual `!`/`!!`, model/settings/tree/compact/reload/session switch/import/fork/resume, arbitrary commands, raw RPC, external editor or secondary user/custom inputs. Reject before command dispatch, queue or context insertion, including non-trigger/nextTurn messages. Paste/shortcuts/report channels do not bypass this. Controls remain outside model context; controller draft remains unchanged.
4. Every internal model round passes private stream admission for incarnation, admitted objective lineage, profile, local denial and known deadline. Guard next-turn preparation, which refreshes model/system/tools. Nonempty objective queues or unapproved user/custom messages deny. No raw execution objects escape; nested model/Pi/provider-launching tools are excluded unless separately covered. Ordinary admitted coding shell remains trusted-local, **not an OS sandbox** or fence on descendants. If transitive hostile shell containment is required, refuse this profile pending a separately authorized sandbox/tool-policy design.
5. Mandatory assertion precedes optional tool hooks and appears at each actual execute wrapper. Parallel preflight does not license later execution after stop/drift. Pure bounded argument preparation/read-only listeners are trusted code; arbitrary preparer/listener effects are not allowed. Local denial is irreversible even when normal loops convert exceptions to tool/assistant errors.
6. Disable automatic/manual compaction, summary, overflow retry, session replacement and first-profile automatic agent/provider retries. Context overflow stops new dispatch and retains attempt/claim. Internal tool-result rounds are permitted descendants, not new initial objectives. Token estimates are disclosed estimates, not exact provider accounting.

## Closed bootstrap effect/resource set

Before task admission allow only: named module/config/context/repo/Git identity reads/stat/realpath/hash; private selected model/auth-presence metadata inspection; the approved AK closed reads/one claim/readback; private reservation/handshake/fresh-session metadata persistence; exact transport, bounded local provenance/presence/reporting and terminal rendering.

No repo writes, package discovery/install/update, model/provider requests, catalog network refresh, credential command, OAuth refresh/login, arbitrary executable resource factory, task-derived bootstrap shell, clipboard/editor injection or payload/credential/env logging. Missing required context refuses even if default discovery would warn and continue. AK's later specified no-maintenance mode bounds nominal read effects; ordinary task-show maintenance is not implicitly accepted.

Use actual pinned instruction semantics including override/worktree behavior and separate SYSTEM/APPEND_SYSTEM selection. Capture required resolved order and omissions; reject invalid/ambiguous decoding. Raw objective, effective instructions/tools/skills and serialization identities are separately bound. Filtering after DefaultResourceLoader discovery cannot undo reads, package resolution or factory effects.

## Same-provider Astra/Codex capability

**Correction/selection:** the inspected openai-responses HTTP path was only a feasibility witness. It is not an alternative provider/account/billing selection. Preserve the exact operator-selected Astra/Codex provider, model and existing subscription/account identity. Static shipped model data lacking an Astra label does not establish actual configured-model availability; no live catalog/account/credential source was inspected.

The inspected native Codex provider is subscription OAuth for ChatGPT, endpoint `https://chatgpt.com/backend-api`. Its `options.apiKey` name carries the existing OAuth bearer; it does not imply switching to API-key billing. The Codex API preserves injected `fetch`, `onPayload`, transport and retry settings through simple/lazy/provider wrappers.

Select an initial **same-Codex SSE-only, no-refresh, no-retry** local profile:

- Host stream admission runs before auth resolution. Use the native Codex serializer/model/reasoning map and a private post-payload validation plus actual outgoing-fetch guard. Force `transport:'sse'` and `maxRetries:0` after all options; reject overrides. No agent/client/native transport retry bypass.
- Bind endpoint, model, reasoning, instructions/input/tools and allowed serialization to the admitted lineage. Actual-send guard rechecks local denial, incarnation, lease/profile/deadline and exact outgoing body. Codex may zstd-compress after onPayload: bounded decompression or an equivalently verified pinned compression mapping is required; a string-only check must not skip byte-array bodies. Refuse redirects/hidden retries. No headers/auth/body diagnostics.
- Use an owner-approved **read-only existing-account OAuth credential snapshot/store**. Refuse mutations/modify callbacks before invoking their updater. Preserve native credential type/toAuth/account identity. Explicit ModelRuntime resources/model metadata, `refreshOnCreate:false`, no default catalog/config network behavior. No login, refresh, account fallback, runtime-key replacement or alternate provider.
- Native auth resolution refreshes within five minutes of expiry, through a separate bare-fetch OAuth path not covered by inference fetch. Plan/run bounds therefore include that refresh threshold plus explicit safety margin, not only task lease. Insufficient lifetime causes `auth_refresh_required` refusal/stop and retained claim/exclusion; account owner may refresh separately before a new lawful attempt. No private snapshot writeback or automatic relaunch.
- Native WebSocket/auto/cached transport uses global socket construction, post-payload cached-delta rewriting, retries and fallback not covered by inference fetch or HTTP maxRetries. They are unsupported. In-run refresh requires a separately accepted guarded native-account rotation/store protocol; WS requires per-instance connection/send guards after rewriting and across reconnect/retry/fallback. No global monkeypatch or unsupported interception claim.

Local SDK credential-store/provider injection makes the restricted same-Codex composition source-feasible without upstream changes. It is **new implementation**, not a shipped profile. Exact requested model/account/provider compatibility, body/auth denial behavior and usable lifetime are post-ADR proof gates. If in-run refresh/WS/full stock parity is a required first-use capability, that is a pre-ADR need-fit change—not a hidden future implementation detail.

## AK interval concurrence and lock ownership

Local Pi **ACCEPTS the proposed AK interval allocation conditionally on this finite protocol**; manual freeze/current per-command gating is not accepted. This is concurrence with supplied AK owner input, not independent AK API verification. AK's acceptance of the exact refinement is recorded separately.

1. Outside DB locking, little-helpers reserves both domains and persists spawn intent. An AK-owned supervisor opens/validates the canonical permanent lockfile **without locking**, then spawns the sealed host with the **same open-file description** and private attempt/incarnation-bound bidirectional channel. A second descriptor opened on a lookalike file is not inherited custody. Descriptor/channel are private process capabilities, not public requests or delegation tokens.
2. Host completes allowed resource/provider-metadata initialization, empty SDK construction/wrappers, frozen context/tools and inert objective staging outside the DB lock. PREPARED means no remaining resource/provider initialization during admission. Initialization failure may deny without ever acquiring the global lock.
3. After PREPARED, supervisor takes canonical Linux flock on the shared OFD and confirms custody. AK performs closed no-maintenance authoritative baseline, one native claim and independent readback in that interval; no recursive ordinary gate or repair fallback. Unknown claim outcome denies without retry.
4. Under the same custody, host durably publishes exactly one immutable local admitted-or-denied **T1 sidecar** within the reserved private attempt. Bind attempt/incarnation, owner observations/baseline digests, local envelope/profile and no-dispatch-yet. Atomic file+directory durability is required. T1 is **not prompt()**, task authority or a replayable permit.
5. **T1 publication must not acquire the namespace mutex under the AK lock.** It is a single-writer host observation; namespace snapshot owns reservation indexes/history and incorporates the sidecar only after DB unlock. This explicitly refines the earlier single-snapshot sketch and avoids AK→namespace inversion.
6. Only after durable T1 may AK explicitly unlock and return correlated CLOSED. Host verifies closure, closes its descriptor as prescribed, projects remaining state outside the lock, then releases the sole guarded objective. Before CLOSED it is admitted-pending-interval-close, not dispatch-enabled. Lost CLOSED or failed post-unlock persistence/deadline denies and preserves claim/exclusion, without reclaim/reacquire/resend.
7. Shared-OFD primitive and canonical lock identity must actually match supported Linux/local filesystem semantics. Ordinary process-associated fcntl locks are not interchangeable. EOF/helper exit does not prove host death. A surviving host retains actual locked OFD custody and latches denial on supervisor loss; neither side performs timeout/PID-based/finally unconditional `LOCK_UN`. Explicit unlock on any shared descriptor unlocks it despite retained references: native helper correctness is essential.
8. Failed T1 durability must not send CLOSED/dispatch; retain/refuse until lawful owner recovery establishes durable denial and closure. If all actual descriptor holders die, the kernel releases its lock, but durable pre-spawn reservation remains unresolved. Do not promise lock persistence after all holders die or infer claim recovery from kernel release.
9. No descriptor/channel reaches UI, arbitrary adapter callbacks, extensions, tools or spawned task commands; close-on-exec except intentional owner/host inheritance. No model/tool child exists before closure. Unknown leaked holders block recovery. Provide out-of-band local inspection that does not need the stranded DB lock. This is local cooperative custody, not hostile same-UID protection.

### AK's accepted finite clarification

The AK delegate accepted this shared-OFD composition with explicit **T2 = unlock**: writer exclusion spans T0–T2; T1 durably binds the exact admitted envelope/claim/baseline or irreversible denial, not merely a supervisor observation. Supervisor verifies one-shot T1 before unlock. CLOSED is private channel/phase-bound, nonpersistent/nonreplayable and carries admitted/denied outcome; denied or missing CLOSED never enables prompt. After T2, the affected-writer stop-and-dispose rule already applies even before first prompt. Lease/phase/incarnation are checked again before dispatch. The detailed concurrence is preserved in the AK owner disposition; no stronger no-authority-mutation-until-provider-send promise is made. All holder/sidecar/projection/failure conditions above remain.

## Namespace, conflict and ABI choices

Provision once through later owner work, binding namespace UUID/root/UID/schema/reader-writer compatibility. Proposed targets (not existing resources):

```text
canonical private root: /home/tryinget/.local/state/pi-task-sessions
fixed owner-installed locator: /home/tryinget/.config/pi-task-sessions/host.json
```

Account identity, not caller HOME/XDG/profile overrides, resolves the root. Validate non-symlink identity, 0700 directories/0600 records and supported atomic-rename/fsync semantics. These paths are proposed provisioning targets, not configured facts. Missing/incompatible state refuses; no competing empty root.

Task key is canonical AK instance+task. Workspace conflict is common-dir equality **OR** identical/nested overlapping checkout **OR** declared shared-effect overlap; retain realpath and physical identities and reject replacement/topology ambiguity. Different common-Git worktrees must not escape via a paired key. Unknown external/nested-repo overlap refuses. Scope globs do not narrow the domain.

Use one permanent private lockfile inode with short Linux advisory flock for atomic durable namespace snapshot checks/reservation indexes/history. Never unlink/recreate it in ordinary operation. Release before AK, spawn, waiting or recovery; provisional reservation remains exclusion. Kernel mutex release is not reservation release. Capacity/history uncertainty refuses rather than dropping blockers. Immutable host T1 is the separate sidecar above. No candidate admission is invoked; other writer enrollment remains explicit.

Retire only after host closure, effect disposition and AK claim resolution. No age/PID/lease/ACK force-unlock. Upgrades first stop new admissions/writers, retain history and a compatible read-only inspector; no online migration/cleanup authorized now. Older versions cannot start empty.

Add same-source emitted Node ESM `./task-session-core`, public bin, private child/state entries and emitted orchestrator adapter closure. Preserve existing TypeScript subpath exports and `pi.extensions`; Node floor remains >=22.19.0 with exact tested Node/SDK versions recorded later. No reliance on runtime TS stripping, parent jiti, source-tree file links or global dependencies; no auto-install. Explicit production exec/model/thinking/cwd/provenance/placement ports never infer test mode from function presence. Test-only override entry is not published in public bin/schema/exports/files. G2-P1 exercises production behavior over the same emitted source.

Keep arbitrary-command observer transport internal. Existing ASC observer is controller-tab-only or headless warning; closing it does not cancel work. Fresh task windows and observer tabs have different policies; no non-Pi caller gains observer fallback.

## Costs and stage boundaries

Whole-Git exclusion, frozen resources, disabled compaction/retries and credential-lifetime bounds can make otherwise useful tasks stop and invite ordinary-Pi bypass. Shared namespace incompatibility affects every supported caller; stranded AK custody affects unrelated coordination. Accept these as explicit first-profile costs, not measured success. Plan discloses omissions/budgets/blockers; later evaluation measures task usefulness, contention, recovery latency, refusal comprehension and workaround pressure separately from race correctness.

Before ADR incorporate these finite local choices and reconcile actual AK/lane dispositions. No further simulated upstream/Pi owner vote is requested. After ADR require construction/ingress/provider/auth/retry/tool-entry negatives, same-OFD/CLOEXEC/T0–T1–CLOSED fault matrix (supervisor alive/host dead, inverse, both dead, lost messages, failed fsync, accidental unlock), source-generation/namespace/candidate/coexistence races, packed CLI+adapter parity, and authorized real visible-session canaries. Nothing above is implementation or proof.

## Selected source identities

P is installed coding-agent root; I is its `node_modules/@earendil-works/pi-ai/dist/`; L is little-helpers and O is orchestrator, relative to pi-extensions. All SHA-256 values identify inspected bytes only.

```text
L package.json 85ee07452f72ba9ef3a2bf1206d739095dbb6471944626f456b75d0341c937c3
L extensions/sidequestLaunch.ts 42ddd7abd8dbcee9598c7abacfb87a62eea91b5c68861438226797e0e0de24a3
L src/candidatePeerAdmissionState.ts e82b0d52a19a9d5b73762428d5d25850676cc564a3d3b2116ce7839b9a6994a9
L src/candidateGitWorktreeIdentity.ts 8a3fb70094cd0d728e34e3735c962471410ad4e6dac442bd3d2410176e56ff72
L docs/project/2026-08-04-asc-execution-observer-contract.md cf7cf7be916ec19a9620247df2d83246b1f2fc829b25686dd8c82b8562eb795d
O package.json c16b660f59ee215ebf37c0e2eaae3ca509402bae08fab87ade199d7f52718ca4
O docs/project/2026-03-31-execution-seam-charter.md 630cd2ae9c7b6f658c713360133ad8e6666138aed9effed416f3a7bd0d760807
P dist/core/sdk.js 6969bd56ba8e1628cd033bb15cb15fe38299f00b5ad84f4f8ef37a33a98681c9
P dist/core/agent-session.js e213e4094a3f176b2491e0470ac8ecd88aeab0030d35aabd9ba289ba7b74b923
P dist/core/model-runtime.js f1d32af458229981db68d587df88d10bce7317f4e4916cd0f04b803d7b64f091
P dist/core/resource-loader.js 8e8a1bc1c5bc9e955f6a2314dd1db02071be56d48b1fea7b8b2cacb4fc9a0628
P node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js 7b75576c0770e8d82c5d74229f5464611d2f1c01b69a6b592eb0862215429f2c
I api/openai-codex-responses.js 0a6904d4869a09a2d9b4144f3364b575306bede11647835dddd9d80032a7f53e
I api/simple-options.js 1caf860e028e22639578aba3ec8b79e5be9650c27bd3f19471c78502976555f3
I providers/openai-codex.js 769490a94a5bcb062c22e768cf0621c91fb1c5d6be3565e6a6fa765eeeabe674
I auth/oauth/openai-codex.js 41432753ef2ec7ef21c577fdc8f83b731c78be90c10ed8e7e1df7010f84d198f
I auth/resolve.js 82ee45ecec319f59536759312a4de25313a8bb8cb7ce43db43d18edc10fef305
```
