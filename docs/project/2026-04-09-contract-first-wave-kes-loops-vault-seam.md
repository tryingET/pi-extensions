---
summary: "Owner-side packet for the current cross-package contract-first wave: bind a thin pi-vault-client prompt-plane seam first, then activate KES and loops in pi-society-orchestrator, and defer higher-order self until lower-plane truth exists."
read_when:
  - "You need the owner-side packet that makes the current seam -> KES -> loops wave executable without re-litigating landing or order."
  - "You are refreshing root direction docs or creating AK tasks for the current cross-package wave."
system4d:
  container: "Owner-side packet for a contract-first cross-package wave inside the pi-extensions monorepo."
  compass: "Bind landing and order truthfully before execution starts."
  engine: "Preserve the routed concern -> make landing explicit -> name the first executable leaf -> keep runtime/schema widening conditional."
  fog: "The main risk is skipping straight to package execution while the seam/order/owner facts are still implicit."
---

# Contract-first wave packet — seam first, then KES, then loops

## 1) Concern Packet Manifest v0

```yaml
concern_title: Bind the cross-package wave contract for a thin pi-vault-client prompt-plane seam first, then pi-society-orchestrator KES/loop activation, while explicitly deferring higher-order self work
packet_mode: contract_first
authority_landing: docs_only
docs_direction_impact: direction_changed_requires_import_check
first_binding_required: Publish this owner-side packet at repo root, refresh the root direction docs to name the seam-first wave as current, then run `./scripts/ak.sh direction import --repo . -F json` and `./scripts/ak.sh direction check --repo . -F json`
evidence_obligations:
  - repo-root owner-side packet doc exists and names the first executable leaf as the thin pi-vault-client prompt-plane seam
  - repo-root strategic, tactical, and operating docs are refreshed to match this packet
  - `./scripts/ak.sh direction import --repo . -F json` succeeds after the docs refresh
  - `./scripts/ak.sh direction check --repo . -F json` succeeds after the docs refresh
compression_target: docs/project/2026-04-09-contract-first-wave-kes-loops-vault-seam.md plus latest repo-root diary capture
knowledge_landing: docs_only
prompt_vault_follow_on: draft_template_delta_spec
```

## 2) Why this landing is truthful

- **owner split:**
  - `pi-vault-client` owns the prompt-plane seam and any thin public runtime or structured contract needed to stop raw prompt-plane bypass
  - `pi-society-orchestrator` owns the next KES/loop control-plane wave after that seam is bound
  - ASC remains the later higher-order self follow-on, not the first-wave owner here
  - docs own the packet and narrative/current-state synthesis
  - `ak direction` owns the structured SG/TG/operating ladder derived from those docs
  - `ak task` owns the first executable leaf after the packet and direction refresh are landed
- **why this is not `execution_first`:**
  - the missing fact was still wave order and first landing; without this packet, tasking could still argue about whether KES, loops, or higher-order self should lead
- **why this is not a premature runtime/schema concern:**
  - existing ADR/stack-map/package-boundary material already defines the authority split; no exact missing canonical fact requires AK-native packet/runtime ownership yet
- **how the routed concern was preserved:**
  - this packet preserves the bounded concern exactly: seam first, KES second, loops third, higher-order self later

## 3) Exact next move

- refresh repo-root direction docs around this packet
- import/check the root direction substrate
- then create the first executable AK tasks for the thin `pi-vault-client` prompt-plane seam

## 4) AK runtime/schema decision now

- `runtime_schema_change_needed_now`: `no`
- `missing_canonical_fact_if_yes`: n/a
- `runtime_candidate_if_yes`: n/a
- `why`: the immediate missing canonical fact is the owner-side packet and direction binding, not an AK-native packet substrate

## 5) Clean Prompt Vault follow-on list

1. `analysis-router` — status: already satisfied upstream; the routed concern is preserved here
2. `contract-first-wave-packet` — status: landed in this doc
3. `repo-direction-to-execution` — status: next; strategic/tactical/operating docs plus `ak direction import/check`
4. `execution-memory-transfer` — status: later; after the first executable seam task is materialized and done
5. `execution-memory-polish` — status: later; after execution evidence exists and needs bounded synthesis/compression
